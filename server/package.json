{
  "scripts": {
    "dev": "tsx index.ts",
    "build": "esbuild index.ts --platform=node --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
